import SynthControlMessage from './SynthControlMessage';
import SynthControlArray from './SynthControlArray';

const packageName = 'synth-control'; // needed for test coverage

export { SynthControlArray, SynthControlMessage, packageName };
